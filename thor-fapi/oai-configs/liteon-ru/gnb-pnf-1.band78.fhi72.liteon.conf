log_config = {
  global_log_level = "info";
  hw_log_level     = "error";
  phy_log_level    = "info";
};

L1s = (
{
  num_cc = 1;
  tr_n_preference  = "nfapi";
  remote_n_address = "127.0.0.1"; // vnf addr
  local_n_address  = "127.0.0.1"; // pnf addr
  local_n_portc    = 50000; // pnf p5 port [!]
  remote_n_portc   = 50002; // vnf p5 port
  local_n_portd    = 50010; // pnf p7 port
  remote_n_portd   = 50011; // vnf p7 port
  prach_dtx_threshold = 130;
  pucch0_dtx_threshold = 80;
  pusch_dtx_threshold = 10;
  max_ldpc_iterations = 10; # 150 for ACC200
  tx_amp_backoff_dB = 20; # needs to match O-RU configuration
  L1_rx_thread_core = 5;
  L1_tx_thread_core = 4;
  phase_compensation = 0; # needs to match O-RU configuration
});


RUs = (
{
  local_rf       = "no";
  nb_tx          = 1; # 4
  nb_rx          = 1; # 4
  att_tx         = 0;
  att_rx         = 0;
  bands          = [78];
  max_pdschReferenceSignalPower = -27;
  max_rxgain                    = 75;
  sf_extension                  = 0;
  eNB_instances  = [0];
  ru_thread_core = 3;
  sl_ahead       = 6;
  tr_preference  = "raw_if4p5"; # important: activate FHI7.2
  do_precoding   = 1; # needs to match O-RU configuration
});

fhi_72 = {
  dpdk_devices = ("0000:70:11.1"); # one VF can be used as well
  system_core = 0;
  io_core = 1;
  worker_cores = (2);
  ru_addr = ("00:11:22:33:44:44");  // VF0 as the middlebox
  mtu = 9600; # note: 1500 works as well, with both E and F releases
  file_prefix = "PNF1";
  fh_config = ({
    T1a_cp_dl = (440, 490); # 85 229
    T1a_cp_ul = (350, 380); # 85 229
    T1a_up = (100, 350); # 100 196
    Ta4 = (110, 180); # 110 180
    ru_config = {
     iq_width = 9;
     iq_width_prach = 9;
    };
    prach_config = {
      eAxC_offset = 4
      kbar = 0;
    };
  });
};

# nrLDPC_coding_aal : {
#   dpdk_dev : "0000:f7:00.1";
#   dpdk_prefix : "PNF1-ACC200";
#   dpdk_core_list : "14-15"; # not used!
#   vfio_vf_token: "00112233-4455-6677-8899-aabbccddeeff";
# };

# loader : {
#   ldpc : {
#     shlibversion : "_aal";
#   };
# };
